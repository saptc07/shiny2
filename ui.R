 

library(shiny)

 
shinyUI(fluidPage(

     
    titlePanel("first app design"),

     
    sidebarLayout(
        sidebarPanel(
            h4("creating user interface"),
            sliderInput("slider",0,50,0)
             
        ),

         
        mainPanel(
            h3("main panel user interface")
            
            

            
            
            
        )
    )get_prod<- reactive({
        input1+input2
        
        get_prod()
)
        
        })
